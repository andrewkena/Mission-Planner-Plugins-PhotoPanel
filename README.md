# PhotoPanel 1.22_10.07.2026 — плагин Mission Planner

Плагин для Mission Planner (проверено на MP 1.3.83), который считает количество
сделанных фотоснимков во время полёта и показывает телеметрию, связанную со
съёмкой: время и координаты последнего снимка, высоту в момент съёмки,
скорость, спутники/точность GPS, средние интервалы между снимками и данные о
следующей путевой точке.

Автор: andrewkena
Версия: 1.22_10.07.2026

## Два варианта плагина

В проекте два файла — это **два разных плагина с одной и той же логикой**,
различается только способ отображения. **В папку `plugins` кладите только
один из них** — иначе оба плагина будут одновременно подписываться на одни и
те же MAVLink-сообщения для одного аппарата (конфликт подписок).

### 1. [Photopanel.cs](PhotoPanel/Photopanel.cs) — вариант со вкладкой
<img width="1280" height="662" alt="photo_2026-07-05_23-45-02" src="https://github.com/user-attachments/assets/33b94ddf-93b2-4b00-afdd-ebbc966510b9" />

Добавляет вкладку **"PhotoPanel"** на экране Flight Data, внизу слева,
рядом со стандартными вкладками Quick/Actions/Servo/... .

Устойчив к запуску SITL: Mission Planner при подключении иногда пересобирает
список вкладок (`tabControlactions.TabPages.Clear()`) и вкладки плагинов при
этом слетают — плагин каждые пол секунды проверяет, что его вкладка на месте,
и возвращает её, если Mission Planner её убрал.

### 2. [PhotoPanelWindow.cs](PhotoPanel/PhotoPanelWindow.cs) — вариант с плавающим окном
<img width="561" height="933" alt="photo_2026-07-05_22-21-34" src="https://github.com/user-attachments/assets/f59d997b-ed92-416a-9464-370ae53b024d" />

Показывает отдельное плавающее окно (`Form`) поверх Mission Planner, которое
можно свободно перетаскивать и менять в размере. Не привязано к вкладкам
Flight Data.

- Окно не показывается в панели задач Windows.
- При закрытии крестиком окно не уничтожается, а прячется — данные
  продолжают накапливаться. При закрытии самого Mission Planner (или
  выгрузке плагина) окно закрывается по-настоящему, иначе зависшая
  скрытая форма не давала бы MP завершить процесс.
- Если окно свернуть (например `Win+↓`), вернуть его можно через пункт
  **"PhotoPanel: показать окно"** в контекстном меню карты (правая кнопка
  мыши по карте на экране Flight Data).

## Что показывает панель

| Поле | Описание |
|---|---|
| Снимков сделано | Счётчик фотографий с начала сессии (крупным шрифтом) |
| Время | Время полёта с момента взлёта (`ЧЧ:ММ:СС`, `Host.cs.timeInAir`) |
| Последнее фото | Координаты последнего снимка (широта, долгота) |
| Высота последнего снимка | Относительная высота (`alt_rel`) в момент съёмки, а не текущая высота аппарата |
| Скорость | Текущая воздушная скорость (м/с) |
| Спутники | Количество спутников, HDOP, VDOP |
| Среднее время между фотографиями | Средний интервал между снимками, сек |
| Среднее расстояние между фотографиями | Среднее расстояние между точками съёмки, м (гаверсинус) |
| Следующий WP | Номер следующей путевой точки маршрута |
| Расстояние до следующего WP | Расстояние до неё, м |

Кнопка **"Сброс"** обнуляет счётчик и всю накопленную статистику (но не
отключает подписку на телеметрию — счёт начинается заново).

## Управление фотографированием

Справа от основного блока показателей, за вертикальным разделителем — три кнопки одна под другой:

| Кнопка | Действие |
|---|---|
| Фото по времени | Запускает покадровую съёмку с интервалом из поля под кнопкой (по умолчанию 1.5 с, минимум 1 с, шаг 0.1 с) — `MAV_CMD_IMAGE_START_CAPTURE` |
| Фото по расстоянию | Запускает съёмку по пройденному расстоянию из поля под кнопкой (по умолчанию 50 м, минимум 50 м) — `MAV_CMD_DO_SET_CAM_TRIGG_DIST` |
| Остановить фото (красная, жирный текст) | Останавливает оба режима съёмки сразу (`MAV_CMD_IMAGE_STOP_CAPTURE` + `DO_SET_CAM_TRIGG_DIST` с дистанцией 0), независимо от того, какой был запущен |

## Источники данных (MAVLink)

- **CAMERA_FEEDBACK** — при получении каждого сообщения увеличивается счётчик
  снимков, сохраняются координаты (`lat`, `lng`), относительная высота
  (`alt_rel`) и время получения; на основе разницы с предыдущим снимком
  считается интервал по времени и расстояние (формула гаверсинуса).
- **CAMERA_CAPTURE_STATUS** (`image_count`) — резервный источник счётчика
  снимков, запрашивается у автопилота раз в секунду (`MAV_CMD_SET_MESSAGE_INTERVAL`).
  `CAMERA_FEEDBACK` — старое сообщение, привязанное к пину обратной связи
  камеры, и может "теряться" в потоке телеметрии при частых кадрах (особенно
  при съёмке по времени, `MAV_CMD_IMAGE_START_CAPTURE`), поэтому итоговое
  число на панели — это максимум из значений `CAMERA_FEEDBACK` и
  `CAMERA_CAPTURE_STATUS`.
- **GPS_RAW_INT** — поле `epv` используется как VDOP (значение `65535`
  трактуется как "нет данных").
- Скорость, спутники, HDOP, номер и расстояние до WP берутся из текущего
  состояния борта (`Host.cs`), а не из CAMERA_FEEDBACK.

Подписки идут только на текущий `sysid`/`compid` (`Host.comPort.sysidcurrent`
/ `compidcurrent`), зафиксированный на момент загрузки плагина.

## Установка

1. Скопировать **один** из файлов (`Photopanel.cs` или `PhotoPanelWindow.cs`)
   в `C:\Program Files (x86)\Mission Planner\plugins\`.
2. Перезапустить Mission Planner — плагин компилируется самим Mission Planner
   при старте, отдельная сборка (`dotnet build`) не требуется.
3. Подключиться к аппарату (реальному или SITL) — вкладка/окно появится
   автоматически.

## Обновление проекта в файле

- `bin/`, `obj/`, `.claude/` не входят в репозиторий (см. `.gitignore`) —
  сборка через `dotnet` не нужна, эти файлы для Mission Planner не имеют
  значения.
- Оба плагина используют один и тот же алгоритм подсчёта расстояния и статистику
  — при изменении логики в одном файле стоит проверить, нужно ли то же
  изменение во втором.
